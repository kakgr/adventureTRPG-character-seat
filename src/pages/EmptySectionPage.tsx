export function EmptySectionPage({ title }: { title: string }) {
  return <div className="page world-page empty-section-page">
    <section className="page-intro"><h1>{title}</h1></section>
  </div>
}
